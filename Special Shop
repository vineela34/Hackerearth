t = int(input())

for l in range(t):

    list1 = []

    n,a,b = map(int,input().split())

    for i, j in zip(range(0,n+1), range(n,-1,-1)):

        list1.append(a*i*i + b*j*j)

    print(min(list1))
